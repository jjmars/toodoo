<% board_id = (@task.status == 1) ? '#doin' : '#done' %>
$("#task_<%= @task.id %>").remove();
$("<%= board_id %> .alert").remove();
$("<%= escape_javascript(render @task) %>").addClass('headShake animated').prependTo("<%= board_id %> .tasks");

